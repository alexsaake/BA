<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Client_ML_Gesture_Sensors.ViewModels"
             x:Class="Client_ML_Gesture_Sensors.Views.GesturePage">
    <ContentPage.BindingContext>
        <model:GestureViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout HorizontalOptions="Center">
                <Label Text="{Binding Message}"/>
                <Button Text="Record" Command="{Binding RecordCommand}"
                        IsEnabled="{Binding Gesture.CanRecord}"/>
                <Button Text="Predict" Command="{Binding GetClusteringCommand}"
                        IsEnabled="{Binding Gesture.IsAvailable}"/>
                <Entry Placeholder="Label" Text="{Binding Label}"
                        IsEnabled="{Binding Gesture.IsNotSaved}"/>
                <Button Text="Save" Command="{Binding SaveCommand}"
                        IsEnabled="{Binding Gesture.IsNotSaved}"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>