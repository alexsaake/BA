<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Sensors_Client.Model"
             x:Class="Sensors_Client.Pages.GeolocationPage">
    <ContentPage.BindingContext>
        <model:GeolocationModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center">
            <Label Text="Geolocation"/>
            <Label Text="{Binding Latitude, StringFormat='Latitude: {0:N2}'}"/>
            <Label Text="{Binding Longitude, StringFormat='Longitude: {0:N2}'}"/>
            <Label Text="{Binding Altitude, StringFormat='Altitude: {0:N2}'}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>