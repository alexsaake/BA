<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Sensors_Client.Model"
             x:Class="Sensors_Client.Pages.OrientationSensorPage">
    <ContentPage.BindingContext>
        <model:OrientationSensorModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center">
            <Label Text="Orientation Sensor"/>
            <Label Text="{Binding X, StringFormat='X: {0:N2}'}"/>
            <Label Text="{Binding Y, StringFormat='Y: {0:N2}'}"/>
            <Label Text="{Binding Z, StringFormat='Z: {0:N2}'}"/>
            <Label Text="{Binding W, StringFormat='W: {0:N2}'}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>